language: cpp
compiler:
  - gcc
  - clang
#before_install :
#  - sudo apt-cache search libboost
#  - sudo apt-get install libboost1.49-dev libboost-python1.49-dev
# Change this to your needs..
script: cmake . && make  externalLibs && cmake . -DWITH_TRWS=TRUE  -DWITH_QPBO=TRUE -DWITH_MRF=TRUE  -DWITH_GCO=TRUE  -DWITH_CONICBUNDLE=TRUE  -DWITH_MAXFLOW=TRUE  -DWITH_MAXFLOW_IBFS=TRUE && make && make test && sudo make install